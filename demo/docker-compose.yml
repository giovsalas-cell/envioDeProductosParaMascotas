version: "1"
services:
  microservicio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    environment:
      - spring.datasource.url=jdbc:oracle:thin:@fullstack1bd_high?TNS_ADMIN=/app/wallet/
      - spring.datasource.username=pelicula
      - spring.datasource.password=FullStack_2026_Java
    restart: always
